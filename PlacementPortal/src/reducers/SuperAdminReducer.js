var INITIAL_STATE = {
    total_students: 20,
    total_companies: 30,
    total_jobs: 50,
}

export default (state = INITIAL_STATE, action) => {
    switch(action.type) {
        default:
            return state;
    }
}